interface:
  display_name: "MotionEyes Animation Debug"
  short_description: "Debug or validate SwiftUI animation and scroll behavior with MotionEyes traces"
  default_prompt: "Use $motioneyes-animation-debug to diagnose or validate a SwiftUI animation or ScrollView behavior with MotionEyes traces."
dependencies:
  tools:
    - type: "mcp"
      value: "xcodebuildmcp"
      description: "Preferred simulator run and log-capture backend for this skill."
policy:
  allow_implicit_invocation: true
